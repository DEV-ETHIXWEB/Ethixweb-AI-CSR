/** DI token for the shared-kernel CircuitBreakerRegistry singleton — see crm.module.ts's providers for the concrete instance. */
export const CIRCUIT_BREAKER_REGISTRY = Symbol("CIRCUIT_BREAKER_REGISTRY");
