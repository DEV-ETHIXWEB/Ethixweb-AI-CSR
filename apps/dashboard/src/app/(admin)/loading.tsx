/** Shown by Next.js automatically while an /admin page's Server Component data fetch (coreApiFetch calls) is in flight. */
export default function AdminLoading() {
  return (
    <div style={{ padding: "48px 32px", color: "var(--ink-soft)", fontSize: "0.85rem" }}>
      Loading…
    </div>
  );
}
